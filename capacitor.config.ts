import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.example.starter',
  appName: 'eduo',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;

